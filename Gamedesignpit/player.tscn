[gd_scene load_steps=4 format=3 uid="uid://h1pw8a5j8b5i"]

[ext_resource type="Texture2D" uid="uid://cng106av0ql4c" path="res://icon.svg" id="1_ef6dx"]

[sub_resource type="GDScript" id="GDScript_s07on"]
script/source = "extends CharacterBody2D


const SPEED = 300.0
const JUMP_VELOCITY = -900.0
#the jump count
var jump_count = 0
var max_jump = 2
# Get the gravity from the project settings to be synced with RigidBody nodes.
var gravity = ProjectSettings.get_setting(\"physics/2d/default_gravity\")


func _physics_process(delta):
	# Add the gravity.
	if not is_on_floor():
		velocity.y += gravity * delta
	if is_on_floor():
		jump_count = 0
	# Handle jump.
	if Input.is_action_just_pressed(\"ui_accept\") and jump_count < max_jump:
		velocity.y = JUMP_VELOCITY
		jump_count = jump_count + 1
	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction = Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)

	move_and_slide()
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_0fn41"]
size = Vector2(128, 128)

[node name="player" type="CharacterBody2D"]
position = Vector2(0, -1)
script = SubResource("GDScript_s07on")
metadata/_edit_group_ = true

[node name="Sprite2D" type="Sprite2D" parent="."]
texture = ExtResource("1_ef6dx")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("RectangleShape2D_0fn41")

[node name="Camera2D" type="Camera2D" parent="."]
position_smoothing_enabled = true
